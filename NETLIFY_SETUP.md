# إعداد المزامنة على Netlify

المشروع يستخدم الآن Netlify Functions وNetlify Blobs لحفظ قاعدة بيانات واحدة مشتركة بين جميع الأجهزة.

## خطوات النشر

1. ارفع **المجلد الرئيسي للمشروع** إلى GitHub ثم اربطه بـ Netlify، أو ارفعه مباشرة إلى Netlify.
2. لا تجعل مجلد `Keycode` هو مجلد النشر؛ ملف `netlify.toml` الصحيح موجود في المجلد الرئيسي.
3. من Netlify افتح: **Site configuration → Environment variables**.
4. أضف متغيرًا باسم `APP_SECRET` وضع له قيمة طويلة وعشوائية، مثل كلمة سر من 32 حرفًا أو أكثر.
5. نفّذ Deploy جديدًا بعد إضافة المتغير.

عند أول تسجيل دخول فقط، ينشئ الموقع مخزن البيانات تلقائيًا. بيانات الدخول الافتراضية هي:

- المستخدم: `admin`
- كلمة المرور: `admin123`

إذا كانت لديك بيانات محفوظة من نسخة سابقة على **نفس رابط Netlify**، افتح الرابط من نفس المتصفح والجهاز وسجّل الدخول أول مرة؛ سينقلها الموقع إلى المخزن السحابي تلقائيًا. أما البيانات الموجودة عند فتح ملف `index.html` محليًا فلها نطاق تخزين مختلف، ولذلك لا يستطيع رابط Netlify قراءتها مباشرة.

بعد ذلك، كل عملية حفظ تُرسل إلى Netlify فورًا، وكل جهاز مفتوح يجلب التعديلات الجديدة كل 15 ثانية. يحتفظ المتصفح أيضًا بنسخة محلية احتياطية عند ضعف الاتصال.
